import ApiService from './api.service'
import URLS from './domain.service'

const EmployeeService = {
  // fetchEmployeeList() {
  //   let axiosOption = Object.assign({})
  //   return ApiService.get(URLS.FETCH_EMPLPOYEE_LISTING(), axiosOption)
  // },
  // fetchEmployeeAccesssDetails() {
  //   let axiosOption = Object.assign({})
  //   return ApiService.get(URLS.FETCH_EMPLOYEE_ACCESS_DETAILS(), axiosOption)
  // }
}

export default EmployeeService
